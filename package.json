{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run cms:dev\" \"npm run web:dev\"",
    "web:dev": "npm run dev --workspace=web",
    "cms:dev": "npm run dev --workspace=cms"
  },
  "workspaces": [
    "apps/**"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
